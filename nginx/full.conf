upstream fastcgi_backend {
    server php:9000;
}

server {
    listen 80;
    server_name magento.local;

    # This is what Magento expects
    set $MAGE_ROOT /var/www/html;
    set $MAGE_MODE developer;

    include /etc/nginx/magento.conf;
}
